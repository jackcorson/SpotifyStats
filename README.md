My very own Spotify Wrapped.

For me:
    Need to export RSPOTIFY_CLIENT_ID, RSPOTIFY_CLIENT_SECRET, and RSPOTIFY_REDIRECT_URI before running.

When running, do something like "cargo run -- current" or "cargo run -- stats month".
